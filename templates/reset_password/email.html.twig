<h1>{{ 'reset_password.email.greeting'|trans }}</h1>

<p>{{ 'reset_password.email.message'|trans }}</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>{{ 'reset_password.email.expiration'|trans({'%time%': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle')}) }}</p>

<p>{{ 'reset_password.email.closing'|trans }}</p>
