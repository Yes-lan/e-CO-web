{% extends 'base.html.twig' %}

{% block title %}{{ 'home.title'|trans }}{% endblock %}

{% block body %}
<div class="container">
    <header class="header">
        <h1>🧭 e-CO Web</h1>
        <p>{{ 'home.subtitle'|trans }}</p>
    </header>
    
    <div class="homepage">
        <section class="hero">
            <h2>{{ 'home.welcome'|trans }}</h2>
            <p>{{ 'home.description'|trans }}</p>
            <div class="cta-section">
                <a href="{{ path('app_course_create') }}" class="btn btn-primary btn-large">
                    ➕ {{ 'home.create_course'|trans }}
                </a>
                <a href="{{ path('app_map') }}" class="btn btn-secondary btn-large">
                    🗺️ {{ 'home.view_map'|trans }}
                </a>
                <a href="{{ path('app_course_manage') }}" class="btn btn-info btn-large">
                    🗂️ {{ 'home.manage_courses'|trans }}
                </a>
            </div>
        </section>
    </div>
</div>
{% endblock %}