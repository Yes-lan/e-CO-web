{% extends 'base.html.twig' %}

{% block title %}{{ isEdit ? 'Modifier le parcours' : 'parcours.create_page_title'|trans }}{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <div class="course-creation-container">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
            <a href="{{ path('app_parcours_list') }}" class="back-link">← {{ 'parcours.back_to_list'|trans }}</a>
        </div>

        <div class="creation-header">
            <h1>📍 {{ isEdit ? 'Modifier le parcours' : 'parcours.create_new_parcours'|trans }}</h1>
            <p>{{ isEdit ? 'Modifiez les informations de votre parcours' : 'parcours.create_subtitle'|trans }}</p>
        </div>
            {{ form(courseForm) }}

        <!--<form id="courseCreationForm">
            




             Informations générales 
            <div class="form-section">
                <h2>📋 Informations Générales</h2>
                
                <div class="form-group">
                    <label for="courseName">Nom du parcours *</label>
                    <input type="text" id="courseName" name="courseName" required 
                           placeholder="Ex: Parcours Forêt de Montmorency">
                </div>

                <div class="form-group">
                    <label for="courseDescription">Description</label>
                    <textarea id="courseDescription" name="courseDescription" 
                              placeholder="Décrivez votre parcours d'orientation..."></textarea>
                </div>
            </div>

            <!-- Point de départ/arrivée 
            <div class="form-section">
                <h2>🚩 Point de Départ/Arrivée</h2>
                
                <div class="info-box">
                    <p>ℹ️ Le point de départ sera également le point d'arrivée du parcours.</p>
                </div>

                <div class="form-group">
                    <label for="startPointName">Nom du point de départ *</label>
                    <input type="text" id="startPointName" name="startPointName" required 
                           placeholder="Ex: Parking de la forêt">
                </div>

                <div class="form-group">
                    <label for="startPointDescription">Description du point de départ</label>
                    <textarea id="startPointDescription" name="startPointDescription" 
                              placeholder="Ex: Parking principal à l'entrée sud de la forêt"></textarea>
                </div>

                <div class="form-group">
                    <label>Position GPS</label>
                    <p style="color: #666; font-size: 0.9rem;">
                        📱 Les coordonnées GPS seront enregistrées lors du scan du QR code par l'enseignant sur le terrain.
                    </p>
                </div>
            </div>

            <!-- Nombre de balises 
            <div class="form-section">
                <h2>🎯 Configuration des Balises</h2>
                
                <div class="form-group">
                    <label for="waypointCount">Nombre de balises (hors départ/arrivée)</label>
                    <div class="waypoint-count-selector">
                        <input type="number" id="waypointCount" name="waypointCount" 
                               min="1" max="20" value="5">
                        <button type="button" class="btn btn-secondary btn-small" id="generateWaypoints">
                            Générer les balises
                        </button>
                    </div>
                </div>

                <div id="waypointsContainer" class="waypoints-container">
                    <!-- Waypoints will be generated here -->
                </div>
            </div>

            <!-- Actions 
            <div class="form-actions">
                <button type="submit" class="btn btn-success">
                    ✅ Créer le parcours
                </button>
            </div> 
        </form>-->
    </div>

    {% block javascripts %}
        
    {% endblock %}
{% endblock %}
